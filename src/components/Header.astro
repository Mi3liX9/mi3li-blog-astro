---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE } from "../consts";
---

<header>
  <nav class="flex justify-between items-center py-4 px-2 shadow-md">
    <h2 class="text-xl font-semibold">
      <a href="/" class="decoration-transparent">{SITE_TITLE}</a>
    </h2>
    <!-- <div>
      <HeaderLink href="/">الرئيسية</HeaderLink>
      <HeaderLink href="/blog">المنشورات</HeaderLink>
      <HeaderLink href="/about">عنّي</HeaderLink>
    </div> -->
    <div class="md:hidden">
      <!-- Social Media Links -->
    </div>
  </nav>
</header>
<style>
  /* header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	.social-links,
	.social-links a {
		display: flex;
	}
	@media (max-width: 720px) {
		.social-links {
			display: none;
		}
	} */
</style>
